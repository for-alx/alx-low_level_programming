#include <stdio.h>
/**
* main - print the result of the multiplication
* @argc: command line argument size
* @argv: command line argument
* Return: 0
*/
int main(int argc, char *argv[])
{
	int result;
	
	if (argc == 3)
	{
		result = atoiargv[1] * atoi(argv[2]));
	}
	printf("%d\n", result);
	return (0);
}
