#include "main.h"
/**
* _isupper - check for uppercase letter
* @c : value to check
* Return : if c in uppercase it will return 1 otherwise 0
**/
int _isupper(int c)
{
	return (c >= 65 && c <= 90);
}
